export type Assistant = {
  name: string;
  voiceCommand: boolean;
  avatar: string;
};

export type AssistantWithUrl = {
  name: string;
  voiceCommand: boolean;
  avatar: string;
  avatarUrl: string;
};
