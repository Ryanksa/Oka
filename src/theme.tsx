import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#5e6cd5",
    },
    info: {
      main: "#4479ce",
    },
    error: {
      main: "#c74b42",
    },
  },
});

export default theme;
